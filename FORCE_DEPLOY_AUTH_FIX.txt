🚨 EMERGENCY: Git merge conflict on server

PROBLEM:
- Server has local changes to controllers/authController.js
- Untracked files blocking merge
- Need to force deploy the auth fix

SOLUTION - Run these commands on server:

# 1. Backup current auth controller (just in case)
cp controllers/authController.js controllers/authController.js.backup

# 2. Remove untracked files that are blocking
rm -f check-server-and-test-notifications.js
rm -f get-token-and-test-notifications.js  
rm -f test-notification-api-on-server.js
rm -f test-notification-data-detailed.js

# 3. Force reset to match GitHub (this will overwrite local changes)
git reset --hard HEAD
git pull origin main

# 4. Restart server immediately
pkill -f "node server.js"
sleep 2
nohup node server.js > server.log 2>&1 &
sleep 3

# 5. Test server
curl -k https://13.48.248.180.nip.io/api/health

# 6. Check if server is running
ps aux | grep "node server.js"

EXPECTED RESULT:
✅ Server starts without Route.get() error
✅ Health endpoint returns 200
✅ Login works with permissions

If server still fails, check logs:
tail -10 server.log