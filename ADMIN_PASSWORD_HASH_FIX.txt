# ADMIN PASSWORD HASH FIX

# Run this on server to create proper bcrypt hash for admin password:

# 1. SSH to server
ssh -i "C:\Users\Admin\awsconection.pem" ubuntu@13.51.56.188

# 2. Create hash for admin@123
node -e "const bcrypt = require('bcrypt'); bcrypt.hash('admin@123', 10, (err, hash) => { console.log('Hash:', hash); process.exit(); });"

# 3. Update admin user with the hash (replace HASH_HERE with actual hash from step 2)
mysql inventory_db -e "UPDATE users SET password = NULL, password_hash = 'HASH_HERE' WHERE email = 'admin@company.com';"

# OR SIMPLER - Just use the working test accounts for demo:
# manager@test.com / manager@123
# operator@test.com / operator@123