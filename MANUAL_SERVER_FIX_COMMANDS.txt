🔧 MANUAL SERVER FIX COMMANDS
============================

Run these commands ONE BY ONE in your terminal:

1. SSH to server:
ssh -i "C:\Users\Admin\awsconection.pem" ubuntu@16.171.197.86

2. Go to project directory:
cd /home/ubuntu/inventoryfullstack

3. Kill any running server:
pkill -f 'node server.js'

4. Fix git conflict:
git config pull.rebase false
git pull origin main

5. Check if files are updated:
ls -la routes/dispatchRoutes.js

6. Start server:
node server.js

If server starts without errors, press Ctrl+C and run:
nohup node server.js > server.log 2>&1 &

7. Check if server is running:
ps aux | grep 'node server.js'

8. Test server:
curl http://localhost:3001/api/auth/test

✅ Server should be working now!